rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all read/write operations to any collection by default.
    // This is a security best practice. If you add more collections,
    // you must add specific rules for them here.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
