rebase# Firebase Studio

This is a NextJS starter in Firebase Studio.

To get started, take a look at src/app/page.tsx.
PWA enabled

## Running AI features

All AI feature routes call OpenRouter from the server. Before running locally or deploying, copy `.env.example` to `.env.local` and set `OPENROUTER_API_KEY`. You can optionally set `OPENROUTER_MODEL` to change the model without editing code.

