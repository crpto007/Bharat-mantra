# Required for all AI feature routes.
# Create this key in OpenRouter and set the same variable in your deployment host.
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Optional: override the default OpenRouter model used by API routes.
# OPENROUTER_MODEL=meta-llama/llama-3.3-70b-instruct:free
